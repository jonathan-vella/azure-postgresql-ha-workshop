# Local Load Test Environment Configuration
# Copy this to .env.local and fill in your PostgreSQL credentials
# Usage: source .env.local (Linux/Mac) or manually set in PowerShell

# PostgreSQL Connection
POSTGRESQL_SERVER=pg-cus.postgres.database.azure.com
POSTGRESQL_PORT=5432
POSTGRESQL_DATABASE=saifdb
POSTGRESQL_USERNAME=jonathan
POSTGRESQL_PASSWORD=your_password_here

# Load Test Configuration
TARGET_TPS=1000
WORKER_COUNT=5
TEST_DURATION=300

# Container Configuration
PORT=80
ASPNETCORE_ENVIRONMENT=Production

# Optional: Docker resource limits
# CPU: number of cores (1.0 for single core, 2.0 for dual core)
# DOCKER_CPUS=1.0
# DOCKER_MEMORY=512m
